{% extends "base.html.twig" %}

{% block head_title %}Rapports de mon unité {{ parent() }}{% endblock %}

{% block head_stylesheets %}
    {{ parent() }}
{% endblock head_stylesheets %}

{% block body_banner %}{% endblock body_banner %}
{% block body_main_container %}
    <h1>Liste des rapports pour {{ app.user.service }}</h1>
    <h2>Rapports en cours d'édition</h2>
    {% set colLength = max(1, (drafts|length)/3) %}
    <div class="row">
    {% for row in drafts|batch(colLength) %}
            <ul>
                {% for rapport in row %}
                    <li>
                        <a href="{{ path('rapport_draft_edit', {'id': rapport.id}) }}">
                            Brouillon {{ rapport.id }} du {{ rapport.lastEdit | date('d/m/Y H:i') }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
    {% endfor %}
    </div>

    <h2>Rapports validés</h2>
{% for row in list|batch(3) %}
    <div class="row">
        {% for title, rapports in row %}
            <div style="width: 33%">
                <h2>{{ title }}</h2>
                <ul>
                    {% for rapport in rapports %}
                        <li>
                            <a href="{{ path('rapport_edit', {'id_edit': rapport.id}) }}">
                                Mission n°{{ rapport.id }} du {{ rapport.dateDebutMission | date('d/m/Y') }}
                            </a>
                            {% if rapport.missions | length > 0 %}
                            <ul>
                                <li>
                                    <a href="{{ path('export_sati') }}"><i class="fas fa-file-export"></i> Export SATI</a>
                                </li>
                            </ul>
                            {% endif %}
                        </li>
                    {% endfor %}
                </ul>
            </div>
        {% endfor %}
    </div>
{% endfor %}


{% endblock body_main_container %}

